# AI Dropshipping System — environment variables
# Copy this file to .env and fill in real values. NEVER commit .env.
#
# Rule: this system only holds keys for research and read-only store access.
# NO auto-spend, auto-refund, or auto-payment keys go here, ever — every
# supplier order, ad spend, and refund is placed by a human, manually.

# AI + research only. NO auto-spend / auto-refund keys.
OPENAI_API_KEY=
SEARCH_API_KEY=

# Optional read-only store key (you approve all actions manually elsewhere)
SHOPIFY_API_KEY=
